---
import { getCollection, render } from 'astro:content';
import DetailLayout from '../../layouts/DetailLayout.astro';

export async function getStaticPaths() {
  const projects = (await getCollection('projects', ({ id }) => id.startsWith('ja/')))
    .sort((a, b) => (a.data.order ?? 99) - (b.data.order ?? 99));
  const b = import.meta.env.BASE_URL;
  return projects.map((project, idx) => ({
    params: { slug: project.id.replace('ja/', '') },
    props: {
      project,
      prevItem: idx > 0
        ? { title: projects[idx - 1].data.title, href: `${b}projects/${projects[idx - 1].id.replace('ja/', '')}/` }
        : null,
      nextItem: idx < projects.length - 1
        ? { title: projects[idx + 1].data.title, href: `${b}projects/${projects[idx + 1].id.replace('ja/', '')}/` }
        : null,
    },
  }));
}

const { project, prevItem, nextItem } = Astro.props;
const { Content } = await render(project);
---

<DetailLayout
  title={project.data.title}
  summary={project.data.summary}
  lang="ja"
  type="project"
  hero={project.data.hero}
  stack={project.data.stack}
  period={project.data.period}
  demoUrl={project.data.demoUrl}
  repoUrl={project.data.repoUrl}
  tags={project.data.tags}
  related={project.data.related}
  prevItem={prevItem}
  nextItem={nextItem}
>
  <Content />
</DetailLayout>
